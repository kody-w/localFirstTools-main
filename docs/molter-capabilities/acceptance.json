{
  "schema": "rappterzoo-molter-acceptance/v1",
  "reference": "Idempotent reconciliation with replay-qualified immutable source handoffs",
  "scope": "The existing Molter controller prepares bounded, preserved review candidates. It does not gain authority to merge or deploy them.",
  "cases": [
    {
      "id": "pure_plan",
      "requirement": "Planning changes no source, registry or publication state and invokes no model or network operation."
    },
    {
      "id": "immutable_capsule_transport",
      "requirement": "The unchanged source-capsule capability restores the selected committed source bytes, not mutable checkout content."
    },
    {
      "id": "qualified_prepare",
      "requirement": "Preparation preserves a complete revision-bound proposal and actual qualification evidence, without claiming submission or deployment."
    },
    {
      "id": "duplicate_request_noop",
      "requirement": "Repeating a completed request returns its existing verified result without regenerating work or minting duplicate evidence."
    },
    {
      "id": "reject_tampered_artifact",
      "requirement": "An intentionally changed proposal, capsule, source identity or qualification receipt is rejected."
    },
    {
      "id": "bounded_allowed_paths",
      "requirement": "Only declared, bounded candidate paths are transported; unsafe paths, uncommitted drift and undeclared changes fail explicitly."
    },
    {
      "id": "reject_metadata_only",
      "requirement": "A failed or empty improvement cannot be promoted as a useful proposal merely because feed, timestamp or frame metadata changed."
    },
    {
      "id": "publication_denied_retains_proposal",
      "requirement": "Denied publication preserves the prepared candidate and truthful state; it cannot trigger a main push, issue closure or false deployment."
    },
    {
      "id": "no_implicit_network_or_model",
      "requirement": "Qualification uses explicitly allowed local checks. A helper, registry label or retry cannot silently invoke a model, remote publication or another scheduler."
    },
    {
      "id": "legacy_frame_contract",
      "requirement": "Existing non-capability Molter entrypoints and failure behavior remain compatible; approval, public-soak and ledger policies are not weakened."
    }
  ],
  "limitations": [
    "Source-capsule qualification proves selected-source transport and replay, not application functionality, adoption, independent repository ownership or deployment.",
    "An independent read-only critic must also exercise the completed output. A passing machine gate alone is not the final review."
  ]
}
